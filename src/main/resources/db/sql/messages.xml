<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <comment>SQL requests to table MESSAGES</comment>

    <entry key="get.all.messages">
        select m.guid, m.date_time, m.dialog_guid, m.sender_guid, m.recipient_guid, m.message
        from MESSAGES m
        where m.dialog_guid = :dialog_guid
        order by m.date_time
        limit :cnt
    </entry>

    <entry key="add.message">
        insert into MESSAGES(guid, date_time, dialog_guid, sender_guid, recipient_guid, message)
        values(:guid, NOW(), :dialog_guid, :sender_guid, :recipient_guid, :message)
    </entry>
</properties>